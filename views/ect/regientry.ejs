<script>
    var stat = "<%= stat %>";
    var errlog = "<%= log %>";

    if(stat == 'Failed'){
        alert('입력한 정보들을 다시 확인해주세요. : ' + errlog);
        location.href = "/register";
    } else {
        alert('회원가입 완료!')
        location.href = "/login";
    }
</script>